### Test with custom property and type
#### Where we define the property and type for all media types
#### Where we don't define neither the property nor type
#### Where we partially define it

### Test with XML property

### Test with bad data in different fields
How does each parser handle it?
