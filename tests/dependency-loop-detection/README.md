Integration Tests for Dependency Loop Finder
============================================
This runs through each sample directory, which must contain a
`expected` file containing the expected return code (0 for no loops, 1
for loops), alongside a number of scheme modules.
